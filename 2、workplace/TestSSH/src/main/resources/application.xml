<?xml version="1.0" encoding="UTF-8" standalone="no"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

	<!-- 配置C3P0数据源 -->
	<bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
		<property name="driverClass" value="${mysql.jdbc.driver}" />
		<property name="jdbcUrl" value="${mysql.jdbc.url}" />
		<property name="user" value="${username}" />
		<property name="password" value="${password}" />
		<property name="minPoolSize" value="${c3p0.minPoolSize}" />
		<property name="maxPoolSize" value="${c3p0.maxPoolSize}" />
		<property name="maxIdleTime" value="${c3p0.maxIdleTime}" />
		<property name="acquireIncrement" value="${c3p0.acquireIncrement}" />
		<property name="initialPoolSize" value="${c3p0.initialPoolSize}" />
		<property name="idleConnectionTestPeriod" value="${c3p0.idleConnectionTestPeriod}" />
		<property name="acquireRetryAttempts" value="${c3p0.acquireRetryAttempts}" />
		<property name="maxIdleTimeExcessConnections" value="${c3p0.maxIdleTimeExcessConnections}" />
		<property name="testConnectionOnCheckin" value="${c3p0.testConnectionOnCheckin}" />
	</bean>

	<!-- 配置会话工厂，读取XML配置的实体 -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate3.LocalSessionFactoryBean">
		<property name="dataSource">
			<ref bean="dataSource" />
		</property>
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">${hibernate.dialect}</prop>
				<prop key="hibernate.show_sql">${hibernate.show_sql}</prop>
				<prop key="hibernate.format_sql">${hibernate.format_sql}</prop>
				<prop key="hibernate.autoReconnection">${hibernate.autoReconnection}</prop>
				<prop key="hibernate.jdbc.batch_size">${hibernate.jdbc.batch_size}</prop>
				<prop key="hibernate.hbm2ddl.auto">update</prop>
			</props>
		</property>
		<property name="mappingResources">
			<list>
				<value>com/test/entity/Account.hbm.xml
				</value>
				<value>com/test/entity/IpEntry.hbm.xml
				</value>
				<value>com/test/entity/Location.hbm.xml
				</value>
				<value>com/test/entity/Person.hbm.xml
				</value>
			</list>
		</property>
	</bean>

	<!-- 配置会话工厂，读取注解实体 -->
	<bean id="amSessionFactory" parent="sessionFactory"
		class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean">

		<property name="annotatedClasses">
			<list>
				<value>com.test.entity.Cat</value>
			</list>
		</property>
		<!-- or below configuration -->
		<property name="packagesToScan">
			<list>
				<value>com.test.entity</value>
			</list>
		</property>
		<!-- <property name="packagesToScan" value="com.mycompany.*.model" > -->
	</bean>

	<!-- 定义事务管理器 -->
	<bean id="transactionManager"
		class="org.springframework.orm.hibernate3.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<bean id="accountDao" class="com.test.dao.AccountDAO">
		<property name="sessionFactory">
			<ref bean="sessionFactory" />
		</property>
	</bean>

	<bean id="accountService" class="com.test.service.AccountService">
		<property name="accountDao">
			<ref bean="accountDao" />
		</property>
	</bean>

	<bean id="personDao" class="com.test.dao.PersonDAO">
		<property name="sessionFactory">
			<ref bean="sessionFactory" />
		</property>
	</bean>

	<bean id="commonService" class="com.test.service.CommonService">
		<property name="personDao">
			<ref bean="personDao" />
		</property>
		<property name="commonDao">
			<ref bean="commonDao" />
		</property>
	</bean>

	<bean id="commonDao" class="com.test.dao.CommonDAO">
		<property name="sessionFactory">
			<ref bean="amSessionFactory" />
		</property>
	</bean>

	<bean id="testService" class="com.test.service.TestService">
		<property name="testDao">
			<ref bean="testDao" />
		</property>
	</bean>

	<bean id="testDaoAgency" class="com.test.dao.impl.TestDaoImpl">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<!-- 代理方式为testDao单独配置事务控制-->
	<bean id="testDao"
		class="org.springframework.transaction.interceptor.TransactionProxyFactoryBean">
		<!-- 配置事务管理器 -->
		<property name="transactionManager" ref="transactionManager" />
		<property name="target" ref="testDaoAgency" />
		<property name="proxyInterfaces" value="com.test.dao.TestDao" />
		<!-- 可选，如果TestService中直接引用的不是TestDao接口而是TestDaoImpl实现类,则必选 -->
		<property name="proxyTargetClass" value="true" />
		<!-- 配置事务属性 -->
		<property name="transactionAttributes">
			<props>
				<prop key="save*">PROPAGATION_REQUIRED</prop>
				<prop key="update*">PROPAGATION_REQUIRED</prop>
			</props>
		</property>
	</bean>

	<!-- 应用配置文件位置信息 -->
	<bean id="jdbc_config"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="location">
			<value>classpath:jdbc.properties</value>
		</property>
	</bean>

</beans>
